// Поиск узла оболочки по идентификатору. Инфраструктура, а не презентация:
// systems и core обращаются к разметке оболочки, но им запрещено импортировать
// `src/ui`, поэтому единственный источник этого доступа живёт здесь.
export const $ = (id: string): HTMLElement | null => document.getElementById(id);
